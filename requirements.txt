# Minimal runtime requirements for Kaggle / local GPU.
# Pin exact versions for camera-ready runs.

numpy>=1.23
scipy>=1.9
scikit-learn>=1.2
tensorflow>=2.16
tqdm>=4.64

# Multi-dataset (MOABB) support
mne>=1.6
moabb>=1.1
pandas>=2.0
pooch>=1.8
